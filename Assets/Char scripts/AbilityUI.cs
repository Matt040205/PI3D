using UnityEngine;
using UnityEngine.EventSystems;
using UnityEngine.UI;

/*public class AbilityUI : MonoBehaviour
{
    [Header("Referências")]
    public Image passiveIcon;
    public Image ability1Icon;
    public Image ability2Icon;
    public Image ultimateIcon;

    public void SetupAbilities(CharacterBase data)
    {
        passiveIcon.sprite = data.passiveAbility.icon;
        ability1Icon.sprite = data.ability1.icon;
        ability2Icon.sprite = data.ability2.icon;
        ultimateIcon.sprite = data.ultimateAbility.icon;

        // Tooltips
        AddTooltip(passiveIcon, data.passiveAbility.description);
        // Repetir para outras habilidades
    }

    void AddTooltip(UIElement element, string text)
    {
        EventTrigger trigger = element.gameObject.AddComponent<EventTrigger>();

        // Configurar eventos de enter/exit para mostrar tooltip
    }
}*/